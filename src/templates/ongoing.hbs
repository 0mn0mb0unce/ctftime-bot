{{#if data.events}}
ongoing events for <b>{{data.current_dt}}</b>:

{{#each data.events as |e|}}
<b>{{e.title}}</b>:
    <i>{{e.format}}</i>
    start: {{e.start_dt}}
    end: {{e.end_dt}}
    time left: {{e.hours_left}}h
    {{e.participants}} participants

{{/each}}
{{else}}
there are no ongoing events for <b>{{data.current_dt}}</b>
{{/if}}
